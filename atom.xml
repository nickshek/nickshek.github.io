<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title><![CDATA[Nick Shek&#039;s Blog]]></title>
    <link href="/atom.xml" rel="self"/>
    <link href="/"/>
    <updated>2016-04-01T00:23:56+08:00</updated>
    <id>/</id>
        <generator uri="http://sculpin.io/">Sculpin</generator>
            <entry>
            <title type="html"><![CDATA[把現有的project folder變為git repository]]></title>
            <link href="/b/把現有的project folder變為git repository"/>
            <updated>2014-12-15T00:00:00+08:00</updated>
            <id>/b/把現有的project folder變為git repository</id>
            <content type="html"><![CDATA[<p>首先，在localhost建立一個假project</p>

<pre><code class="bash">cd ~/
mkdir project1
cd project1/
echo "HelloWorld!" &gt;&gt; test.txt
</code></pre>

<p>驗證是否確實將Hello World 寫入test.txt</p>

<pre><code class="bash">cat test.txt
</code></pre>

<p>建立git repository</p>

<pre><code class="bash">git init
</code></pre>

<p>在remote host 建立bare repository,以下的blog詳細解釋了什麼是bare git repository</p>

<p><a href="http://www.saintsjd.com/2011/01/what-is-a-bare-git-repository/">http://www.saintsjd.com/2011/01/what-is-a-bare-git-repository/</a></p>

<pre><code class="bash">cd ~/
mkdir project1.git
cd project1.git/
git init --bare
</code></pre>

<p>在localhost,</p>

<pre><code class="bash">cd ~/project1
</code></pre>

<p>新增remote origin url</p>

<pre><code class="bash">git remote add origin ssh://&lt;username&gt;@&lt;ip address&gt;/&lt;path to bare repo&gt;/project1.git
</code></pre>

<p>萬一新增的url 有錯，可以透過下列指令更新remote origin url</p>

<pre><code class="bash">git remote set-url origin &lt;url&gt;
</code></pre>

<p>最后，輸入我們最熟悉的git指令</p>

<pre><code class="bash">git push origin master
</code></pre>
]]></content>
        </entry>
            <entry>
            <title type="html"><![CDATA[令你的youtube embed code變為responsive]]></title>
            <link href="/b/令你的youtube embed code變為responsive"/>
            <updated>2014-12-10T00:00:00+08:00</updated>
            <id>/b/令你的youtube embed code變為responsive</id>
            <content type="html"><![CDATA[<p>把youtube的Embed HTML由</p>

<pre><code class="html">&lt;iframe src="//www.youtube.com/embed/AsHKOH03mxw" frameborder="0" height="315" width="560"&gt;&lt;/iframe&gt;
</code></pre>

<p>改成</p>

<pre><code class="html">&lt;div class="video-container"&gt;
  &lt;iframe src="//www.youtube.com/embed/AsHKOH03mxw" frameborder="0" height="315" width="560"&gt;&lt;/iframe&gt;
&lt;/div&gt;
</code></pre>

<p>之後加入以下CSS</p>

<pre><code class="css">.video-container {
    position: relative;
    padding-bottom: 56.25%;
    padding-top: 30px; height: 0; overflow: hidden;
}

.video-container iframe,
.video-container object,
.video-container embed {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
}
</code></pre>
]]></content>
        </entry>
            <entry>
            <title type="html"><![CDATA[HKTV 選戰 插曲]]></title>
            <link href="/b/HKTV 選戰 插曲"/>
            <updated>2014-12-10T00:00:00+08:00</updated>
            <id>/b/HKTV 選戰 插曲</id>
            <content type="html"><![CDATA[<p>值得你付出8元去下載這首歌以支持香港音樂</p>

<p>ITunes下載連結<a href="https://itunes.apple.com/hk/album/ill-be-there-hktv-ju-ji-xuan/id946282372?i=946282373">陳詩慧 Eva Chan - I’ll Be There</a></p>

<div class="video-container">
  <iframe width="560" height="315" src="https://www.youtube.com/embed/AsHKOH03mxw" frameborder="0" allowfullscreen></iframe>
</div>
]]></content>
        </entry>
            <entry>
            <title type="html"><![CDATA[在ubuntu移除圖片的metadata]]></title>
            <link href="/b/在ubuntu移除圖片的metadata"/>
            <updated>2014-12-07T00:00:00+08:00</updated>
            <id>/b/在ubuntu移除圖片的metadata</id>
            <content type="html"><![CDATA[<p>1.安裝exiftool:</p>

<pre><code class="bash">sudo apt-get install libimage-exiftool-perl
</code></pre>

<p>2.讀取圖片資訊</p>

<pre><code class="bash">exiftool example.jpg
</code></pre>

<p>3.移除圖片資訊</p>

<pre><code class="bash">exiftool -all= example.jpg
</code></pre>

<p>4.原本的圖片會保留成example.jpg_original</p>
]]></content>
        </entry>
            <entry>
            <title type="html"><![CDATA[在rails migration改變column type]]></title>
            <link href="/b/在rails migration改變column type"/>
            <updated>2014-11-26T00:00:00+08:00</updated>
            <id>/b/在rails migration改變column type</id>
            <content type="html"><![CDATA[<p>假設我們要改變sub_anonrums table的title,將其欄位種類由:text改為:string方法如下:</p>

<p>1.在project下新增一個migration file</p>

<pre><code class="bash">rails g migration change_sub_anonrums_title_format
</code></pre>

<p>2.移除migration 下的change 方法,改為使用up 和down方法,migration file 的source code 如下所示: \</p>

<pre><code class="ruby">class ChangeSubAnonrumsTitleFormat &lt; ActiveRecord::Migration
  def up
    change_column :sub_anonrums, :title, :string
  end

  def down
    change_column :sub_anonrums, :title, :text
  end
end
</code></pre>

<p>3.執行 rake db:migrate</p>
]]></content>
        </entry>
            <entry>
            <title type="html"><![CDATA[使用ubuntu在localhost建立subdomain]]></title>
            <link href="/b/使用ubuntu在localhost建立subdomain"/>
            <updated>2014-11-13T00:00:00+08:00</updated>
            <id>/b/使用ubuntu在localhost建立subdomain</id>
            <content type="html"><![CDATA[<p>有好多時候，我們需要在localhost測試多個project無論是(php,Ruby,Python的項目)。在localhost建立subdomain可以讓我們在同一主機測試不同項目:</p>

<p>php項目1的URL : http://localhost/project1/</p>

<p>php項目2的URL : http://localhost/project2/</p>

<p>我們的最終目的是改變以上的項目的URL為:</p>

<p>php項目1的URL : http://project1.localhost/</p>

<p>php項目2的URL : http://project2.localhost/</p>

<p>1.首先修改/etc/hosts:</p>

<pre><code class="bash">sudo nano /etc/hosts
</code></pre>

<p>在/etc/hosts加入以下原始碼:</p>

<pre><code>127.0.0.1       project1.localhost
127.0.0.1       project2.localhost
</code></pre>

<p>2.在/etc/apache2/sites-available/新增project1和project2的configure file:</p>

<p>project1.conf</p>

<pre><code class="apache">&lt;VirtualHost *:80&gt;
  # The ServerName directive sets the request scheme, hostname and port that
  # the server uses to identify itself. This is used when creating
  # redirection URLs. In the context of virtual hosts, the ServerName
  # specifies what hostname must appear in the request's Host: header to
  # match this virtual host. For the default virtual host (this file) this
  # value is not decisive as it is used as a last resort host regardless.
  # However, you must set it for any further virtual host explicitly.
  ServerName project1.localhost

  ServerAdmin webmaster@localhost
  DocumentRoot /var/www/html/project1

  # Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
  # error, crit, alert, emerg.
  # It is also possible to configure the loglevel for particular
  # modules, e.g.
  #LogLevel info ssl:warn

  ErrorLog ${APACHE_LOG_DIR}/error.log
  CustomLog ${APACHE_LOG_DIR}/access.log combined

  # For most configuration files from conf-available/, which are
  # enabled or disabled at a global level, it is possible to
  # include a line for only one particular virtual host. For example the
  # following line enables the CGI configuration for this host only
  # after it has been globally disabled with "a2disconf".
  #Include conf-available/serve-cgi-bin.conf
&lt;/VirtualHost&gt;

# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
</code></pre>

<p>project2.conf</p>

<pre><code class="apache">&lt;VirtualHost *:80&gt;
    # The ServerName directive sets the request scheme, hostname and port that
    # the server uses to identify itself. This is used when creating
    # redirection URLs. In the context of virtual hosts, the ServerName
    # specifies what hostname must appear in the request's Host: header to
    # match this virtual host. For the default virtual host (this file) this
    # value is not decisive as it is used as a last resort host regardless.
    # However, you must set it for any further virtual host explicitly.
    ServerName project2.localhost

    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html/project2

    # Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
    # error, crit, alert, emerg.
    # It is also possible to configure the loglevel for particular
    # modules, e.g.
    #LogLevel info ssl:warn

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined

    # For most configuration files from conf-available/, which are
    # enabled or disabled at a global level, it is possible to
    # include a line for only one particular virtual host. For example the
    # following line enables the CGI configuration for this host only
    # after it has been globally disabled with "a2disconf".
    #Include conf-available/serve-cgi-bin.conf
&lt;/VirtualHost&gt;

# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
</code></pre>

<p>3.用a2ensite令該二個virtual hosts的設定生效:</p>

<pre><code class="bash">sudo a2ensite /etc/apache2/sites-available/project1.conf
sudo a2ensite /etc/apache2/sites-available/project2.conf
</code></pre>

<p>4.輸入sudo service apache2 restart重新啟動apache2</p>
]]></content>
        </entry>
    </feed>